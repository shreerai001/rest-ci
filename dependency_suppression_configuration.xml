
<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <!-- 
        Suppress false positives for dependencies used only in non-production environments
    -->
    
    <!-- H2 Database - Test Environment Only -->
    <suppress>
        <notes><![CDATA[
            H2 database is only used in test environment with console disabled.
            Production environment uses PostgreSQL which is not affected.
            Security risk is minimal as test environment is not publicly accessible.
        ]]></notes>
        <gav regex="true">^com\.h2database:h2:.*$</gav>
    </suppress>
    
    <!-- Spring Boot DevTools - Development Only -->
    <suppress>
        <notes><![CDATA[
            Spring Boot DevTools is only included in development scope.
            Not packaged in production builds.
        ]]></notes>
        <gav regex="true">^org\.springframework\.boot:spring-boot-devtools:.*$</gav>
    </suppress>
</suppressions>
